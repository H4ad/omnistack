/**
 * O enum para guardar as informações das chaves usadas para acessar o localStorage
 */
export enum KeysEnum {

  /**
   * A key para guardar o token proxy
   */
  TOKEN_PROXY = 'TOKEN_PROXY_KEY',

  /**
   * A key para guardar as informações do usuário
   */
  USER_PROXY = 'USER_PROXY_KEY',
}
